# ─── Anthropic + Agno (AgentOS + Agent framework) ────────────────────────────
anthropic>=0.40.0
beautifulsoup4>=4.12.0
agno==2.5.9

# ─── FastAPI + servidor ───────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12        # upload de arquivos

# ─── Supabase ─────────────────────────────────────────────────────────────────
supabase>=2.10.0
psycopg[binary]>=3.2.0          # driver PostgreSQL para Agno PgStorage
sqlalchemy>=2.0.0               # requerido por agno.db.postgres

# ─── Processamento de documentos ──────────────────────────────────────────────
docling>=2.15.0                 # extração de texto de PDFs

# ─── Geração de PDF ───────────────────────────────────────────────────────────
weasyprint>=62.3
markdown>=3.6           # markdown → html no pipeline de debriefing

# ─── Agendamento (agente de rotina) ───────────────────────────────────────────
apscheduler>=3.10.4

# ─── HTTP ─────────────────────────────────────────────────────────────────────
httpx>=0.27.0
requests>=2.32.0

# ─── Google Drive (debriefings — extrai docs do cliente) ──────────────────────
google-api-python-client>=2.140.0
google-auth>=2.34.0

# ─── Utilitários ──────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-jose[cryptography]>=3.3.0  # validação JWT Supabase (fallback)
